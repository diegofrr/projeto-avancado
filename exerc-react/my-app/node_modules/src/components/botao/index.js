import React, { Component } from 'react';

class Botao extends Component {

    constructor(props) {
        super(props);
        this.state = {
            texto: props.texto
        }


    }

    render() {
        return (
            <button>
            {this.state.texto}
            </button>
        )
    }

}

export default Botao;